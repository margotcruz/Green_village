<form method="post" action="{{ path('app_image_produit_delete', {'id': image_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_produit.id) }}">
    <button class="btn">Delete</button>
</form>
