{% extends 'base.html.twig' %}

{% block title %}Echange index{% endblock %}

{% block body %}
    <h1>Echange index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateEchange</th>
                <th>SujetEchange</th>
                <th>TypeEchange</th>
                <th>DescriptionEchange</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for echange in echanges %}
            <tr>
                <td>{{ echange.id }}</td>
                <td>{{ echange.dateEchange ? echange.dateEchange|date('Y-m-d') : '' }}</td>
                <td>{{ echange.sujetEchange }}</td>
                <td>{{ echange.typeEchange }}</td>
                <td>{{ echange.descriptionEchange }}</td>
                <td>
                    <a href="{{ path('app_echange_show', {'id': echange.id}) }}">show</a>
                    <a href="{{ path('app_echange_edit', {'id': echange.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_echange_new') }}">Create new</a>
{% endblock %}
